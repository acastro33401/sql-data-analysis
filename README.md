# Data Analysis Portfolio — Alan Castro

Finance & Business Analytics graduate (University of Delaware, 2024) with hands-on experience in SQL, Python, and data analysis. This repository contains academic and project work demonstrating data querying, cleaning, visualization, and machine learning applied to real-world datasets.

---

## SQL — Database Queries
**`sql_portfolio.sql`**  
Structured query language work from MISY 330 (Database Design & Implementation). Covers:
- Filtering, range queries, and pattern matching (`WHERE`, `BETWEEN`, `LIKE`, `IN`)
- Aggregation and grouping (`COUNT`, `AVG`, `MAX`, `SUM`, `GROUP BY`, `HAVING`)
- Sorting and deduplication (`ORDER BY`, `DISTINCT`)
- Joins — `INNER JOIN`, `LEFT JOIN`, multi-table joins
- Subqueries

---

## Python & Machine Learning — Jupyter Notebooks

### Finance & Business Applications

**`Decision_Trees_Loans.ipynb`**  
Decision tree classifier applied to a loan dataset — predicting loan outcomes based on borrower characteristics. Covers model training, evaluation, and feature importance.

**`Linear_Regression_Car_Price.ipynb`**  
Multivariate linear regression to predict car prices. Includes data cleaning, feature selection, multicollinearity analysis, and train/test split with k-fold cross-validation.

**`KMeans_Donation.ipynb`**  
K-Means clustering applied to donor data to identify distinct giving segments — useful for targeted outreach and resource allocation.

**`IRS_Dashboard.ipynb`**  
Exploratory data analysis and visualization of IRS tax return data. Includes data aggregation, descriptive statistics, and charts built with Matplotlib/Seaborn.

### Academic Projects

**`Project6_DecisionTree_Titanic.ipynb`**  
Decision tree model on the Titanic dataset — a classic binary classification problem. Includes preprocessing, model building, and accuracy evaluation.

**`FinalP_Set_AlanCastro.ipynb`**  
Final problem set from MISY 445 (Data Science for Finance) — covers multiple ML techniques applied to structured financial datasets.

---

## Skills Demonstrated
- **Languages:** Python, SQL
- **Libraries:** Pandas, NumPy, Scikit-learn, Matplotlib, Seaborn
- **Techniques:** Data cleaning, EDA, regression, classification, clustering, cross-validation
- **Tools:** Jupyter Notebook, Excel, Power BI

---

*University of Delaware — MISY 330 (Database Design & Implementation), MISY 445 (Data Science for Finance)*
